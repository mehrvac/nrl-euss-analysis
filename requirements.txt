# Runtime dependencies (single source of truth mirrored in pyproject.toml).
# Install the project itself with: pip install -e .
pandas>=2.0
pyarrow>=14
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
matplotlib>=3.7
seaborn>=0.12
boto3>=1.28
pvlib>=0.10
pyyaml>=6.0
tqdm>=4.65
tabulate>=0.9
pytest>=7.4
